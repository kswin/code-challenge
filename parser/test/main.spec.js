// var expect = require('chai').expect;
// var fs = require('fs');
// var main = require('../src/main');

// describe('Main', function(){
//     describe('csvToJson', function(){
//         it('should throw error if filename with incorrect extension is entered', function(){
//             expect(main.csvToJson.bind(main, 'my-filename.txt')).to.throw(Error);
//         });

//         it('should NOT throw error if filename with incorrect extension is entered', function(){
//             expect(main.csvToJson.bind(main, 'my-filename.csv')).not.to.throw(Error);
//         });

//         it('should convert csv entry to json with csv header as the keys and column data as the values');
//     });
// });